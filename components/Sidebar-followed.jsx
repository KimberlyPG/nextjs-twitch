import StreamerLive from "./Streamer-live";

const SidebarFollowed = ({ dataTrue, dataFalse}) => {
    return (
        <div>
            {/* <div>
                <img className="rounded-full h-10" src={dataTrue.profile_image_url} alt="" />
                <h1 className="pl-2">{dataTrue.display_name}</h1>
            </div>
            <div className="flex flex-row pb-3">
                <img className="grayscale rounded-full h-10" src={dataFalse?.profile_image_url} alt="" />
                <h1 className="pl-2">{dataFalse?.display_name}</h1>
            </div> */}
        </div>
    )
}

export default SidebarFollowed;